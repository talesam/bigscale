.DS_Store
node_modules
/build
/web
/.svelte-kit
/package
.env
.env.*
!.env.example
*.tar.gz

# Bind-mount runtime data — never commit users' SQLite, keys, or admin.json
/data
/backups

# Local AI agent state — never share
/.claude