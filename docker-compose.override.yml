# Override usado em desenvolvimento/teste local.
# Em produção, suba só com `docker-compose.yml` (sem este arquivo).
services:
  bigscale:
    ports:
      - "3000:3000"     # painel admin em http://localhost:3000
      - "18080:8080"    # API/engine na 18080 do host (8080 fica livre pro dev local)
